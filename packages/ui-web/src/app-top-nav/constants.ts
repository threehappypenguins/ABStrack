/**
 * Default URL path for the ABStrack logo served from each Next.js app's `public/` folder.
 * Canonical raster lives at `packages/ui-web/assets/logo.png`; copy or sync into
 * `apps/web/public/logo.png` and `apps/practitioner/public/logo.png` when the asset changes.
 */
export const ABSTRACK_APP_LOGO_SRC = '/logo.png';

/**
 * Secondary line under “ABStrack” on user and practitioner web chrome (nav, public header).
 * Matches marketing copy: Auto-Brewery Syndrome Tracking.
 */
export const ABSTRACK_WEB_TAGLINE = 'Auto-Brewery Syndrome Tracking';
