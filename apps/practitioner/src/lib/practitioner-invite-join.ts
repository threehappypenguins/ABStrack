/** Post–email-invite landing path (PKCE callback `next` and default redirect). */
export const PRACTITIONER_INVITE_JOIN_PATH = '/invite/join';

/** `update-password?from=` value for the post-invite password flow. */
export const PRACTITIONER_INVITE_SET_PASSWORD_FROM = 'practitioner-invite';
