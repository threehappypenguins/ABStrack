/**
 * Query value for `?from=` on `/update-password` when the caretaker opened it right after
 * accepting a patient invite (session already established; no recovery email required).
 */
export const CARETAKER_INVITE_SET_PASSWORD_FROM = 'caretaker-invite';
