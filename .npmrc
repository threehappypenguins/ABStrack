# Hoist PowerSync packages so Metro/pnpm resolve a single `@powersync/common` tree where the SDK pulls it in.
# Scoped names are `@scope/name`; `*@powersync/*` does not match them — use `@powersync/*`.
public-hoist-pattern[]=@powersync/*
