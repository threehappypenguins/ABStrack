# Hoist PowerSync packages so TypeScript sees one physical `@powersync/common` (Schema/Table types stay nominally compatible across `@abstrack/powersync` and `@powersync/react-native`).
public-hoist-pattern[]=*@powersync/*
